import { useContext } from "react";
import UserContext from "../contexts/userContext";
import { useNavigate } from "react-router-dom";

function Home() {
  //   can i ask 1 question in here AM i Logged in ? yes

  return (
    <>
      <h1>Home page Welcome here !!!</h1>
    </>
  );
}

export default Home;
